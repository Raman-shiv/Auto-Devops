Select Start > Settings> System > Display > Brightness. Clear the Change brightness automatically when lighting changes check box (if it appears), and then use the Change brightness slider to set the brightness level you want.

On Windows 11:

Select the Start > Settings > System > Display. Turn off Change brightness automatically when lighting changes (if it appears), and then use the Change brightness slider to set the brightness level you want.

Open Display settings
New operations are in progress
